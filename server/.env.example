# This .env.example file provides a template for the server’s environment
# variables.  Copy it to `.env` in this directory and fill in the values.

# --- OpenAI configuration ---
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE

# --- Stripe configuration ---
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
STRIPE_PRICE_ID=price_your_price_id_here

# Optional: If you do not specify STRIPE_PRICE_ID, the server will use an
# inline price defined by STRIPE_AMOUNT (in the smallest currency unit)
# and STRIPE_CURRENCY (e.g. usd, aud).  For example, to charge $10.00
# USD, set STRIPE_AMOUNT=1000 and STRIPE_CURRENCY=usd.
#STRIPE_AMOUNT=1000
#STRIPE_CURRENCY=usd

# Domain for constructing Stripe success/cancel URLs.  Set this to your
# deployed domain (e.g. https://myapp.com) or leave blank for local dev.
#DOMAIN=http://localhost:5000